// Simple script for landing page interactions
document.addEventListener('DOMContentLoaded', () => {
    // Add any landing page specific interactions if needed
    console.log('IM2015 Premium Prediction Tool loaded');
});
